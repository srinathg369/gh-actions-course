name: 02-workflow events

on: push

jobs: 
    echo:
        runs-on: ubuntu-latest
        steps:
        - name: show the trigger event
          run: echo "i am trigered by ${{github.event_name}}"


