name: 01 - building blocks

on: workflow_dispatch

jobs: 
    echo-world:
        runs-on: ubuntu-latest
        steps:
            - name: say hello
              run: echo "echo world"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: failed step
              run: |
                echo "I will success"
                exit 0
            - name: say good bye
              run: echo "Good bye"


    
              
